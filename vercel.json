{
  "version": 2,
  "buildCommand": "pip install -r requirements.txt",
  "env": {
    "PYTHON_VERSION": "3.11"
  },
"functions": {
    "vertex_app/backend/**/*.py": {
      "includeFiles": "{PROJECT/**,trained_models/**,pipeline_common.py}"
    }
  }
}